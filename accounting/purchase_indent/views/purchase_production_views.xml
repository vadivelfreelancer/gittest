<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<data>
		<record id='view_purchase_to_purchase_indent_form' model='ir.ui.view'>
			<field name="name">purchase.indent.form</field>
			<field name="model">purchase.indent</field>
			<field name="arch" type="xml">
				<form string="Indents" create="false" edit="false" delete="false">
					<header>
						<button name="purchase_indent_confirm" string="Confirm" states="draft" type="object"
							class="oe_highlight" />
						<field name="state" widget="statusbar"
							statusbar_visible="draft,waiting_approval,inprogress,move_created,done"
							statusbar_colors='{"reject":"red"}' />
					</header>
					<sheet>
						<h1>
							<field name="name" class="oe_inline" readonly="1"/>
						</h1>
						<group>
							<group>
								<field name="requirement"/>
                                <field name="issued_by" />
                               <field name="company_id" groups="base.group_multi_company"/>
							</group>
							<group>
								<field name="indent_date" />
								<field name="required_date" />
								<field name="issued_date" />
							</group>
						</group>
						<notebook>
							<page string="Moves">
								<field name="move_lines">
									<tree string="Products" colors="grey:state == 'cancel';green:state == 'done';red:(state not in ('cancel','done')) and date > current_date;black:(state not in ('cancel','done'))">
                                        <field name="product_id"/>
										<field name="name"/>
										<field name="product_uom_qty"/>
										<field name="product_uom" groups="product.group_uom"/>
                                        <field name="state" invisible="1"/>
                                        <field name="location_id"/>
                                        <field name="location_dest_id"/>
										<field name="purchase_indent_id" invisible="1"/>
										<field name="date" invisible="1"/>
                                    </tree>
								</field>
								<div class="oe_clear">
									<label for="description" />
								</div>
								<field name="description" class="oe_inline" placeholder="Additional note..." />
							</page>
							<page string="Other Info">
								<group>
									<group><field name="item_for"/></group>
								</group>
							</page>
						</notebook>
					</sheet>
				</form>
			</field>
		</record>

		<record id='view_purchase_to_purchase_indent_tree' model='ir.ui.view'>
			<field name="name">purchase.indent.tree</field>
			<field name="model">purchase.indent</field>
			<field name="arch" type="xml">
				<tree string="Indents" create="false" edit="false" delete="false"
					colors="red:(state == 'waiting_approval' and  requirement == '2') or (state == 'inprogress' and  requirement == '2');black:state=='draft';green:state=='inprogress';gray:state=='reject';">
					<field name="name" />
					<field name="indent_date" />
					<field name="required_date" />
					<field name="requirement" invisible="1"/>
					<field name="state" />
				</tree>
			</field>
		</record>

		<record id="act_purchase_production_indent_view" model="ir.actions.act_window">
            <field name="name">Indent Orders</field>
            <field name="res_model">purchase.indent</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
			<field name="view_ids"
                   eval="[(5, 0, 0),
                          (0, 0, {'view_mode': 'tree', 'view_id': ref('view_purchase_to_purchase_indent_tree')}),
                          (0, 0, {'view_mode': 'form', 'view_id': ref('view_purchase_to_purchase_indent_form')})]"/>
            <field name="domain">[('origin', '=', active_id)]</field>
        </record>
    </data>
</odoo>
